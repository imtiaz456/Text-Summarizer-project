transformers
transformers[sentencepiece]
dataset
sacrebleu
rouge_score
py7zr                    #The 7z file format is a popular archive and compression format in recent days.
pandas                   # This module provides tools to read, write and list 7z file.
nltk
tqdm
pyYAML
matplotlib
torch  # if we have gpu then we can install the torch gpu version
notebook
boto3
mypy-boto3-s3
python-box == 6.0.2
ensure == 1.0.2
fastapi == 0.78.0
uvicorn == 0.18.3
jinja2 == 3.1.2
-e .
